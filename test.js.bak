'use strict';

/**
    * @module
*	 @exports
*/

function show() {
    return JSON.stringify({
        'name': 'Pizza-Hub Menu'
    });
}

function showAnother() {
    return JSON.stringify({
        'name': '8 Inch Pizza',
        'price': '$10.00'
    });
}

console.log(`${show()}, ${showAnother()}`);
